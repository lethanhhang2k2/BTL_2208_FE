export default function Logo({ size = "32px" }) {
    return (
        <div
            className="font-logo text-[#325ab1]"
            style={{ fontSize: size }}>
            Tiro
        </div>
    )
}